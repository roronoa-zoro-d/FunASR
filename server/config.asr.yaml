# This file was generated using the `serve build` command on Ray v2.10.0.

proxy_location: EveryNode

http_options:

  host: 0.0.0.0

  port: 8000

grpc_options:

  port: 9000

  grpc_servicer_functions: []

logging_config:

  encoding: TEXT

  log_level: INFO

  logs_dir: null

  enable_access_log: true

applications:

- name: app1

  route_prefix: /asr/paraformer

  import_path: ray_asr_paraformer_large:asr_app

  runtime_env: {}

  deployments:

  - name: ASR_Engine
    num_replicas: 1
    ray_actor_options:
      num_cpus: 1.0
      num_gpus: 0.3

- name: app2

  route_prefix: /asr/whisper_large

  import_path: ray_asr_whisper_large:asr_app

  runtime_env: {}

  deployments:

  - name: ASR_Engine
    num_replicas: 1
    ray_actor_options:
      num_cpus: 1.0
      num_gpus: 1

- name: app3

  route_prefix: /asr/whisper_large_turbo

  import_path: ray_asr_whisper_large_turbo:asr_app

  runtime_env: {}

  deployments:

  - name: ASR_Engine
    num_replicas: 1
    ray_actor_options:
      num_cpus: 1.0
      num_gpus: 1

- name: app4

  route_prefix: /vad/fsmn

  import_path: ray_vad_fsmn:vad_app

  runtime_env: {}

  deployments:

  - name: VAD_Engine
    num_replicas: 1
    ray_actor_options:
      num_cpus: 1.0
      num_gpus: 0.3
